fileVersion: 1
id: "1"
macroString: |-
  {#-- The below block of code initialises variables in case of node typess using advance deployment strategy #}

  {% if desiredState %}
      {% set columns = desiredState.columns %}
      {% set storageLocations = desiredState.storageLocations %}
      {% set config = desiredState.config %}
      {% set sources = desiredState.sources %}
      {% set node = desiredState.node %}
      {% set parameters = desiredState.parameters %}
  {% endif %}

  {#-- This macro will frame the order by clause with the columns and sort order specified in config #}
  {#-- Input parmeters   - None #}
  {#-- Return            - Order by clause #}

  {%- macro sortorder_by_colv(return) -%}
      {%- set nsVariables = namespace(sortcolNames="") -%}
      {%if config.orderby %}
          {%- for i in config.orderbycolumn.get('items') -%}
              {%- set colName = i.sortColName.name -%}
              {%- set colOrder = i.sortOrder -%}
  	        {%- if loop.first -%}	      
  		        {%- set nsVariables.sortcolNames = '"' + colName + '"' + colOrder -%}	  
  	        {%- else -%}      
  		        {%- set nsVariables.sortcolNames = nsVariables.sortcolNames + ',' + '"' + colName + '"' + colOrder-%}	  
  	        {%- endif -%}
          {%- endfor -%}	
              {%- set nsVariables.sortcolNames = 'order by' + nsVariables.sortcolNames -%}
   
      {% endif %} 
      
      {{- nsVariables.sortcolNames -}}
   
  {%- endmacro -%}

  {#-- The below block of code frmaes the partition by clause for qualify function #}
  {#-- Input parmeters   - None #}
  {#-- Return            - partition by clause #}

  {%- macro partition_by() -%}

      {%- set nsVariables = namespace(partition="") -%}
      {%- set sourceTableName = node.name -%}

      {%- for i in config.partitionBy.get('items') -%}
          {%- set colName = i.partColName.name -%}
          {%- if loop.first -%}
              {%- set nsVariables.partition = getSourceNameFromColumnName(colName) + '."' + colName + '"' -%}
          {%- else -%}
              {%- set nsVariables.partition = nsVariables.partition + ',' + getSourceNameFromColumnName(colName) + '."' + colName + '"' -%}
          {%- endif -%}

      {%- endfor -%}

      {{- nsVariables.partition -}}
  {%- endmacro -%}

  {%- macro getColumnId( colName ) -%}

      {%- set columnId = columns | selectattr('name','equalto',colName) | map(attribute='id') | first -%}

      {{- columnId -}}

  {%- endmacro -%}

  {% macro getSourceNameFromColumnName( colName ) %}

      {%- set sourceObject = sources[0].columns | selectattr('id','eq',getColumnId( colName )) | map(attribute='sourceColumns')  | first | map(attribute='node.name') | first -%}

      "{{- sourceObject -}}"

  {% endmacro %}


  {#-- This macro will split the join clause into from and rest of the clauses like where,groupby,orderby ,qualify  #}
  {#-- Input parmeters   - joinclause #}
  {#-- Return            - from clause and other clauses #}


  {%- macro get_clause( joinclause , ret_clause ) -%}

      {#  STEP 1: Remove Block Comments /* ... */ --#}
      {#  We split by the start of the comment and only keep the part after the end of the comment #}
      {%- set ns_block = namespace(text="") -%}
      {%- set block_parts = joinclause.split('/*') -%}
      {%- set ns_block.text = block_parts[0] -%} {# The text before the very first /* #}
      
      {%- for part in block_parts[1:] -%}
          {%- if '*/' in part -%}
              {# Split by the first occurrence of */ and take everything after it #}
              {%- set after_comment = part.split('*/', 1)[1]  -%}
              {%- set ns_block.text = ns_block.text ~ ' ' ~ after_comment  -%}
          {%- endif -%}
      {%- endfor -%}

      {# STEP 2: Remove Line Comments #}
      {%- set ns_line = namespace(clean_lines=[]) -%}
      {%- for line in ns_block.text.split('\n') -%}
          {%- set active_part = line.split('--')[0] -%}
          {%- set ns_line.clean_lines = ns_line.clean_lines + [active_part] -%}
      {%- endfor  -%}
      
      {# Rejoin everything and normalize whitespace for searching #}
      {%- set cleaned_join_clause = ns_line.clean_lines | join(' ')  -%}
      {%- set search_string = ' ' ~ cleaned_join_clause.lower().replace('\t', ' ').replace('\r', ' ') ~ ' '  -%}

      {# STEP 3: Identify Keyword Split Point (Priority: WHERE > GROUP > QUALIFY > ORDER) #}
      {%- set found_keyword = '' -%}
      {%- set split_index = -1 -%}

      {%- if ' where ' in search_string -%}
          {%- set found_keyword = 'where'  -%}
          {%- set split_index = search_string.find(' where ') -%}
      {%- elif ' group by ' in search_string -%}
          {%- set found_keyword = 'group by' -%}
          {%- set split_index = search_string.find(' group by ') -%}
      {%- elif ' qualify ' in search_string -%}
          {%- set found_keyword = 'qualify' -%}
          {%- set split_index = search_string.find(' qualify ') -%}
      {%- elif ' order by ' in search_string -%}
          {%- set found_keyword = 'order by' -%}
          {%- set split_index = search_string.find(' order by ') -%}
      {%- endif -%}

      {# STEP 4: Perform the Split #}
      {%- if split_index >= 0 -%}
          {# split_index maps directly to cleaned_join_clause because we replaced chars 1-to-1 #}
          {%- set from_part = cleaned_join_clause[:split_index] -%}
          
          {# offset = keyword length + 1 space #}
          {%- set offset = found_keyword|length + 1 -%}
          {%- set remainder = cleaned_join_clause[split_index + offset:] -%}

          {%- if found_keyword == 'where' -%}
              {%- set add_part = 'AND ' ~ remainder -%}
          {%- else -%}
              {%- set add_part = found_keyword ~ ' ' ~ remainder -%}
          {%- endif -%}
      {%- else -%}
          {%- set from_part = cleaned_join_clause -%}
          {%- set add_part = '' -%}
      {%- endif -%}

      {# STEP 5: Return Result #}
      {%- if ret_clause == 'from' -%}
          {{ from_part | trim }}
      {%- else -%}
          {{ add_part | trim }}
      {%- endif  -%}

  {%- endmacro -%}


  {%- macro ref_raw(location_name, node_name) -%}
      {%- raw -%}{{ ref('{% endraw %}{{- location_name }}{% raw %}', '{% endraw %}{{ node_name }}{% raw %}') }}{% endraw %}
  {%- endmacro -%}


  {%- macro ref_no_link_raw(location_name, node_name) -%}
      {% raw %}{{ ref_no_link('{% endraw %}{{ location_name }}{% raw %}', '{% endraw %}{{ node_name }}{% raw %}') }}{% endraw %}
  {%- endmacro -%}

  {#-- The below block of code initializes metadata changes variables for the display #}
  {#-- ===============================================================================#}
  {% macro update_metadata(tag, diff, label) %}
      {% set nsMetaVariables.metadataUpdate = nsMetaVariables.metadataUpdate + '-' + tag %}

      {# Handle metaDataChanges #}
      {% if nsMetaVariables.metaDataChanges %}
          {% set nsMetaVariables.metaDataChanges = nsMetaVariables.metaDataChanges ~ "\n\n" ~ label ~ " Changes:\n" ~ diff %}
      {% else %}
          {% set nsMetaVariables.metaDataChanges = label ~ " Changes:\n" ~ diff %}
      {% endif %}

  {% endmacro %}

  {###########################################################################################
  Purpose: Determines the correct Snowflake DROP object syntax based on the materialization type.
   
  Input: materialization (string): The Snowflake object type to be dropped
    (e.g., table, view, materialized view, dynamic table, external table).
   
  Returns: string: The appropriate DROP object keyword with IF EXISTS
    (e.g., 'table IF EXISTS', 'view IF EXISTS', 'materialized view IF EXISTS').
  #############################################################################################}
  {% macro dropMaterialization(materialization) %}
      {%- if materialization | lower in ['table', 'transient table','temporary table', 'hybrid table'] -%}
          TABLE IF EXISTS
      {%- elif materialization | lower in ['view', 'secure view', 'run view', 'cortex search service'] -%}
          VIEW IF EXISTS
      {%- elif materialization | lower in ['dynamic table','transient dynamic table', 'dynamic iceberg table'] -%}
          DYNAMIC TABLE IF EXISTS
      {%- elif materialization | lower == 'materialized view' -%}
          MATERIALIZED VIEW IF EXISTS
      {%- elif materialization | lower == 'external table' -%}
          EXTERNAL TABLE IF EXISTS
      {%- elif materialization | lower == 'iceberg table' -%}
          ICEBERG TABLE IF EXISTS
      {%- elif materialization | lower == 'interactive table' -%}
          INTERACTIVE TABLE IF EXISTS
      {%- elif materialization | lower == 'stream' -%}
          STREAM IF EXISTS
      {%- else -%}
          TABLE IF EXISTS
      {%- endif %}
  {% endmacro %}

  {###########################################################################################
  Purpose: Generates SQL-commented warnings or UI notifications when a user attempts to 
  change the Node Type or Materialization Type of a node. It identifies 
  critical node configurations (Tasks, Streams, Hybrid Views, Cortex, etc.) 
  that could lead to data loss or broken dependencies if switched.

  Input: None

  Returns: A string containing a SQL comment warning (starting with '-- ⚠️') 
      and/or triggers a stage-level warning UI notification for critical switches.
  #############################################################################################}
  {% macro getNodeTypeSwitchWarning() %}
      {%- if currentState.node.nodeType.split(':::')[-1] in ['300', '301'] -%}
          {# Current Node Type: Deferred Merge Nodes #}
          -- ⚠️ Warning: This table may be linked to streams and hybrid views. Any required updates/drops must be handled manually.
      {%- elif currentState.node.nodeType.split(':::')[-1] in ['549', '550', '154', '155'] -%}
          {# Current Node Type: Root and Resume Tasks Nodes #}
          -- ⚠️ Warning: These node types are not ideal for switching, as they may act as root or resume tasks. Changing them may disrupt related tasks, cause inconsistencies, or result in data loss. Any required updates/drops must be handled manually.
      {%- elif currentState.node.nodeType.split(':::')[-1] in ['416', '448'] -%}
          {# Current Node Type: Data Quality related statis table/view #}
          -- ⚠️ Warning: This node is used for data quality checks and may not represent the final static schema. Deployment or execution could fail or interrupt email notifications and task failures.
      {%- elif currentState.node.materializationType | lower in ['task'] or currentState.node.nodeType.split(':::')[-1] in ['429', '567'] -%}
          {# Current Node Type: Tasks Related, Insert or Merge with Task, Document AI Nodes#}
          -- ⚠️ Warning: Tasks or streams are attached to this node. Changing its type may cause task failures, stream inconsistencies, or data loss. Any required updates/drops must be handled manually.
      {%- elif currentState.node.materializationType | lower in ['stream'] -%}
          {# Current Node Type: Stream Nodes #}
          -- ⚠️ Warning: This is a stream node. Changing its type may cause task failures, stream inconsistencies, or data loss. Any required updates/drops must be handled manually.
      {%- elif currentState.node.materializationType | lower in ['cortex search service'] -%}
          {# Current Node Type: Cortex Nodes #}
          -- ⚠️ Warning: Cortex search service is attached to this node. Changing its type may cause service failures, inconsistencies, or data loss. Any required updates/drops must be handled manually.
      {%- endif %}
  {% endmacro %}
  {# ----------------------------------------------------------------------
    Macro: safe_metadata

    Purpose: Handles failures during Advanced Deploy metadata updates when dynamic
    Jinja logic is present in node configurations.

    Specifically addresses cases where:
      - PreSQL or PostSQL contains embedded Jinja expressions (e.g., {{ ... }})
      - Join clauses include conditional Jinja logic (e.g., {% if ... %})
  ---------------------------------------------------------------------- #}
  {% macro safe_metadata(value) %}
      {% if value is not none %}
          {{ value
              | replace("{%", "{ %")
              | replace("%}", "% }")
              | replace("{{", "{ {")
              | replace("}}", "} }")
              | replace("{#", "{ #")
              | replace("#}", "# }")
              | replace("/*", "/ *")
              | replace("*/", "* /")
              | replace("--", "- -")
          }}
      {% else %}
          {{ value }}
      {% endif %}
  {% endmacro %}

  {#=========================================================
    Initialize variables for V2 node types that use the 
    advanced deployment strategy
  ===========================================================#}
  {%- macro initializeNodeVariables(nsNodeVariables) -%}

    {%- if currentState is defined -%}
      {%- set currentDatabase = currentState.storageLocations 
                                | selectattr('name', 'equalto', currentState.node.location.name)
                                | map(attribute='database')
                                | first -%}
      {%- set currentSchema = currentState.storageLocations
                              | selectattr('name', 'equalto', currentState.node.location.name)
                              | map(attribute='schema')
                              | first -%}
      {%- set nsNodeVariables.currentDatabaseSchema = currentDatabase + '.' + currentSchema -%}

      {%- set nsNodeVariables.curObjectNameFullPath = ref_no_link(currentState.node.location.name, currentState.node.name) -%}
    {%- endif -%}

      {%- if desiredState is defined -%}
      {%- set desiredDatabase = desiredState.storageLocations
                                | selectattr('name', 'equalto', desiredState.node.location.name)
                                | map(attribute='database')
                                | first -%}
      {%- set desiredSchema = desiredState.storageLocations
                                | selectattr('name', 'equalto', desiredState.node.location.name)
                                | map(attribute='schema')
                                | first -%}
      {%- set nsNodeVariables.desiredDatabaseSchema = desiredDatabase + '.' + desiredSchema -%}

      {%- set nsNodeVariables.desObjectNameFullPath = ref_no_link(desiredState.node.location.name, desiredState.node.name) -%}

    {%- endif -%}

  {%- endmacro -%}

  {# ========================================================================
  Description:
  Generates a correctly formatted SQL default value based on the provided datatype
  and input value. Ensures proper quoting, function handling, and datatype alignment
  for use in DDL/DML statements.

  Inputs:
  - dataType (string):
      Target column datatype (e.g., VARCHAR, NUMBER, DATE, BOOLEAN, VARIANT)

  - defaultValue (any):
      Default value to be applied (can be string, number, boolean, NULL, or function)

  Output:
  - Returns a SQL-safe default value string formatted according to datatype
  ========================================================================#}
  {%- macro get_default(dataType, defaultValue) -%}

      {%- if dataType is not none -%}

          {%- set dtype = dataType | upper -%}
          {%- set default_str = defaultValue | string -%}
          {%- set is_quoted = default_str.startswith("'") and default_str.endswith("'") -%}
          {%- set quote_types = [
              'VARCHAR', 'STRING', 'TEXT', 'CHAR', 'DATE', 'TIME', 'VARIANT', 'OBJECT', 'ARRAY', 'GEOGRAPHY', 'GEOMETRY', 'XML', 'BINARY'
          ] -%}
          {%- set upper_default = default_str | upper -%}
          {%- set is_function =
              not is_quoted
              and (
                  upper_default in [
                      'CURRENT_DATE',
                      'CURRENT_TIME',
                      'CURRENT_TIMESTAMP',
                      'CURRENT_USER',
                      'CURRENT_ROLE'
                  ]
                  or upper_default.startswith('SYSDATE(')
                  or upper_default.startswith('CURRENT_TIMESTAMP(')
                  or upper_default.startswith('TO_DATE(')
                  or upper_default.startswith('TO_TIME(')
                  or upper_default.startswith('TO_TIMESTAMP(')
                  or upper_default.startswith('TO_TIMESTAMP_NTZ(')
                  or upper_default.startswith('TO_TIMESTAMP_LTZ(')
                  or upper_default.startswith('TO_TIMESTAMP_TZ(')
                  or upper_default.startswith('UUID_STRING(')
                  or upper_default.startswith('RAND(')
                  or upper_default.startswith('RANDOM(')
              )
          -%}
          {%- set is_null = default_str | upper == 'NULL' -%}
          {%- set is_boolean = default_str | upper in ['TRUE', 'FALSE'] -%}
          {%- set is_number = default_str.replace('.', '', 1).isdigit() -%}

          {%- if is_null -%}
              NULL

          {%- elif is_function -%}
              {{ default_str }}

          {%- elif is_boolean -%}
              {{ default_str | upper }}

          {%- elif is_number and dtype not in quote_types -%}
              {{ defaultValue }}

          {%- elif is_quoted -%}
              {{ default_str }}

          {%- else -%}
              {%- for qt in quote_types -%}
                  {%- if dtype.startswith(qt) -%}
                      '{{ default_str | escape }}'
                  {%- endif -%}
              {%- endfor -%}
          {%- endif -%}
      {%- endif -%}
  {%- endmacro -%}

  {# ========================================================================
  Purpose          : Executes data quality tests associated with the node
                      and its columns, based on the configured run order.
                      Supports running tests either before or after data
                      insertion, and respects the test continuation
                      behavior on failure.
  Input Parameters : runOrder (String)
                      - Determines when the tests should be executed.
                          Expected values: 'Before' or 'After'
  Returns          : Rendered SQL statements for executing the applicable
                      node-level and column-level data quality tests.
                      No output is generated if tests are disabled.
  ========================================================================= #}
  {%- macro run_tests(runOrder) -%}

      {%- if config.testsEnabled is defined and config.testsEnabled == true -%}
          {%- if runOrder == 'After' -%}
              {%- for column in columns -%}
                  {%- if column.tests is defined -%}
                      {%- for test in column.tests | default([]) -%}
                          {%- if test is mapping and test.parameters is defined -%}
                              {%- if test.parameters[0] | trim | lower == "null" -%}
                                  {%- set templateString = "SELECT 1 FROM " +  ref(node.location.name, node.name)  + " WHERE " + column.name + " IS NULL " -%}                      
                                  {{ test_stage("Column Test: " + column.name + " - " + test.parameters[0] | lower) }}
                                  {{ templateString }}
                              {%- endif -%}
                              {%- if test.parameters[0] | trim | lower == "unique" -%}
                                  {%- set templateString = "SELECT 1 FROM " + ref(node.location.name, node.name) + " GROUP BY " + column.name +  " HAVING COUNT(*) > 1" -%}                      
                                  {{ test_stage("Column Test: " + column.name + " - " + test.parameters[0] | lower) }}
                                  {{ templateString }}
                              {%- endif -%}
                          {%- endif -%}
                      {%- endfor -%}
                  {%- endif -%}
              {%- endfor -%}
          {%- endif -%}

          {%- set ns = namespace(cnt=0) -%}
          {%- for test in config.tests | default([]) -%}
              {%- if test is mapping and test.parameters is defined -%}
                  {%- if test.parameters[1] | default("Before") == runOrder -%}
                      {%- set ns.cnt = ns.cnt + 1 -%}
                      {{ test_stage(
                          ('Pre Load Test ' if runOrder == 'Before' else 'Post Load Test ') ~ ns.cnt,
                          test.parameters[2] | default(true)
                      ) }}
                      {{ test.parameters[0] }}
                  {%- endif -%}
              {%- endif -%}
          {%- endfor -%}

      {%- endif -%}
  {%- endmacro -%}

  {# ========================================================================
  Purpose:
      Returns the SQL expression to be used for a column transformation.

  Input:
      col
          - transform: Column transform expression.
          - sourceColumns[0].node.name: Source table name.
          - sourceColumns[0].column.name: Source column name.

  Logic:
      1. If transform is not empty and matches the source column name
          (ignoring double quotes), return a fully qualified source
          column reference:
              "SOURCE_TABLE"."SOURCE_COLUMN"

      2. If transform is not empty and does not match the source column
          name, return the transform expression as-is.

      3. If transform is empty, return NULL.

  Examples:
      transform = '"N_NAME"'      -> "NATION_TEST"."N_NAME"
      transform = 'N_NAME'        -> "NATION_TEST"."N_NAME"
      transform = 'UPPER(N_NAME)' -> UPPER(N_NAME)
      transform = ''              -> NULL
  ========================================================================#}
  {%- macro get_source_transform_v2(col) -%}
      {%- set transform = col.transform | trim -%}
      {%- if transform != '' -%}
          {%- if col.sourceColumns
                  and col.sourceColumns[0].node
                  and transform | replace('"', '')
                      == col.sourceColumns[0].column.name -%}

              "{{ col.sourceColumns[0].node.name }}"."{{ col.sourceColumns[0].column.name }}"
          {%- else -%}
              {{ transform }}
          {%- endif -%}
      {%- else -%}
          NULL
      {%- endif -%}
  {%- endmacro -%}

  {#===================================================================================================
  Description:
  Generates a deterministic hash value by combining and hashing all column values associated with the given `hash_name` using the `@inHash` annotation.

  Input: `hash_name` (string) - The hash group identifier used in `@inHash("<hash_name>")` to group columns for hashing.

  Output: - Returns a SQL expression of type `STRING`:
          - A SHA1 hash of concatenated column values  
          - Returns `NULL` if no columns are mapped to the given hash group
  ====================================================================================================#}
  {%- macro get_hash(hash_name, algo='SHA1', delimiter='||', datatype='CHAR(32)') -%}

      {%- set cols = namespace(items=[]) -%}
      {%- set algo_upper = algo | upper -%}

      {# Collect columns participating in this hash #}
      {%- for col in sources[0].columns -%}

          {%- if col.inHash is defined -%}
              {%- for p in col.inHash -%}
                  {%- set parts = p.parameters[0] %}
                  {%- set position = p.parameters[1] | int -%}
                  {%- set hash = p.parameters[0] -%}

                  {%- if hash == hash_name -%}
                      {%- set cols.items = cols.items + [{
                          "position": position,
                          "column": get_source_transform_v2(col)
                      }] -%}
                  {%- endif -%}
              {%- endfor -%}
          {%- endif -%}
      {%- endfor -%}

      {%- if cols.items | length == 0 -%}
          NULL
      {%- else -%}
          {%- set sorted_cols = cols.items | sort(attribute='position') -%}
          {# Define algo wrappers #}
          {%- set algo_start = algo_upper + '(' -%}
          {%- set algo_end = ')' -%}

          {%- if algo_upper == 'SHA256' -%}
              {%- set algo_start = 'SHA2(' -%}
              {%- set algo_end = ', 256)' -%}
          {%- endif -%}

          CAST(
              {{ algo_start }}
                  {%- for c in sorted_cols -%}
                      NVL(CAST({{ c.column }} AS VARCHAR), 'null')
                      {%- if not loop.last %}
                          || {%- if delimiter != '' %} '{{ delimiter }}' || {% endif -%}
                      {%- endif -%}
                  {%- endfor -%}
              {{ algo_end }}
          AS STRING)
      {%- endif -%}
  {%- endmacro -%}

  {####################################################################################################################################
    Macro : get_clusterby_v2

    Description:
      Generates the CLUSTER BY clause for the nodes cluster configuration. Supports both column-level
      clusters and transform expressions (e.g., TRUNCATE, BUCKET).

    Inputs (from node config):
      - config.clusterKey      : boolean — enables/disables clustering
      - config.clusterKeyConfig.items: list of cluster entries, each containing:
          - columnNameExpressions     : transform expression (e.g., TRUNCATE(10, n_name), BUCKET(6, n_nationkey))
          - columnName.name           : plain column name used when no expression is defined (e.g., n_regionkey)

    Output:
      - If toggle is ON and valid clusters exist:
          CLUSTER BY (TRUNCATE(10, n_name), n_regionkey, BUCKET(6, n_nationkey))
      - If toggle is OFF or no valid clusters found:
          '' (empty string)
  ####################################################################################################################################}
  {%- macro get_clusterby_v2() -%}

      {%- if config.clusterEnabled is defined and config.clusterEnabled == true -%}
          {%- set cols = namespace(items=[]) -%}
          {# Collect columns participating in this clustreBy #}
          {%- for col in sources[0].columns -%}
              {%- if col.isClusterKey is defined -%}
                  {%- for cluster in col.isClusterKey -%}
                      {%- set cols.items = cols.items + [{
                              "position":  cluster.parameters[0],
                              "column": cluster.parameters[1] | default(col.name, true)
                          }] -%}
                  {%- endfor -%}
              {%- endif -%}
          {%- endfor -%}

          CLUSTER BY (
              {%- if cols.items | length > 0 -%}
                  {{ cols.items
                      | sort(attribute='position')
                      | map(attribute='column')
                      | join(', ')
                  }}
              {%- endif -%}
          )
       {%- endif -%}

  {%- endmacro -%}

  {####################################################################################################################################
  getNewColumnName(oldName)

  Description:
  Returns the current column name corresponding to a previous column name.

  Input:
  oldName (String) - Previous column name.

  Output:
  String - Current column name if found; otherwise an empty string.
  ####################################################################################################################################}
  {% macro getNewColumnName(oldName) -%}
      {%- set ns = namespace(newName='') -%}
      {%- for col in columns -%}
          {%- set prevNames = (col.previousName | default({})).parameters | default([]) -%}
          {%- if prevNames and prevNames[0] == oldName -%}
              {%- set ns.newName = col.name -%}
          {%- endif -%}
      {%- endfor -%}
      {{- ns.newName -}}
  {%- endmacro %}
name: macro
type: Macro
